import { TextOptionsLight } from 'jspdf';

export const textOptions: TextOptionsLight = {
  align: 'left',
  maxWidth: 185,
};

export const pageOptions = {
  margin: 20,
  padding: 5,
  footerHeight: 15,
  xPos: 10,
};
